<%= form_for(client_bed) do |f| %>
  <% if client_bed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_bed.errors.count, "error") %> prohibited this client_bed from being saved:</h2>

      <ul>
      <% client_bed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
